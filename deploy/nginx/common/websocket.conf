# WebSocket upgrade, at http level.
#
# The Angular dev server and the chat both need it, and a proxy that does not
# pass these two headers renders the UI and then hangs with no replies.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
