# The routes every environment shares. Included inside a server block.
#
# Upstreams are named, not addressed: each environment file defines `site`,
# `content`, `analysis`, `deltasignal` and `chatbot` to suit itself — localhost
# ports on a host, service names in compose, and something else again when node
# in this repository takes over what Tomcat serves.
#
# What is NOT here is anything that differs by environment: bot blocking, rate
# limiting, TLS, and the Tina admin all live in the environment files, because
# getting one of them wrong in the wrong place is the expensive kind of mistake.
#
# The admin rule in particular is deliberately repeated in each environment
# rather than shared. nginx cannot choose an include by variable, and the
# workaround would have hidden the one decision that must be obvious at a glance:
# whether a CMS editor is reachable from the internet.

# The endpoints the node port serves (tools/content-node), matched exactly so
# they win over the /ContentService/ prefix below -- nginx takes the longest
# match, and `=` beats any prefix, so unlike Apache the order in this file does
# not decide it.
#
# These three are every path Java exposes under /data/content/, so a prefix
# would work today. Still named individually: a prefix would silently claim the
# next endpoint Java adds and turn a working page into a 404, and three lines
# are a cheap price for a route that cannot acquire new meaning on its own.
#
# Byte-identical to Java bar two declared differences, both written down beside
# the code that makes them: a subpathway's DOI, which ContentPageManager
# discards by passing null into TocSubpathway, and the order of the contributors
# array, which the page sorts client-side before anyone sees it. See specs/006
# D11, D12 and D15. All three lists are held in memory and answer in ~4ms.
#
# To fall back: delete the block for that endpoint. Java answers again with the
# next reload, and the client-side join in the contents page still covers the
# DOIs.
location = /ContentService/data/content/toc {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content_node;
}

location = /ContentService/data/content/doi {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content_node;
}

location = /ContentService/data/content/contributors {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content_node;
}

# The species lists, byte-identical to Java and verified by the diff harness
# over the whole corpus rather than by eye.
#
# Exact matches for the same reason as the three above, and a sharper one here:
# Java serves other shapes under /data/species/ that node does not, so a prefix
# would claim them and 404 what works today.
location = /ContentService/data/species/main {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content_node;
}

location = /ContentService/data/species/all {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content_node;
}

# The person page's four lists, which take an id so they cannot be exact
# matches. `~` is a regex location and those are tried in order before any
# prefix, so this wins over /ContentService/ below without depending on where in
# the file it sits -- but it is written tightly enough that it can only ever
# claim these four names.
#
# One declared difference, and it fixes a bug rather than introducing one: Java
# returns an event once per authorship edit, so a reaction edited twice by the
# same person appears twice. The person page showed 3,310 authored reactions for
# one curator where /content/contributors counts 3,309, and listed that reaction
# twice. Node returns each event once.
location ~ ^/ContentService/data/person/[^/]+/(authored|reviewed)(Pathways|Reactions)$ {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content_node;
}

# The exporters that actually draw something, which are the expensive part of
# ContentService and the only part with a history of taking the origin down:
# crawlers on these URLs exhausted Tomcat's heap. They have had no budget of
# their own until now -- only the site-wide 100r/s, which is sized for page
# assets.
#
# Named individually rather than as `/exporter/`, because that prefix is not all
# one thing. `/exporter/reaction/{id}/diagram` is a 2.9 KB JSON document served
# in 0.17s and it is on the critical path for every reaction page; a render of
# R-HSA-109606 is 454 KB in 0.55s, and at full quality 7 MB. Measured, after
# writing the prefix version and realising it would throttle reaction pages to
# protect something they do not use.
#
# A regex location, so it is tried before the `/ContentService/` prefix below and
# anything not listed here -- the reaction JSON included -- falls through to it
# unbudgeted.
#
# Shares `render_rate` with /RenderService/ deliberately. The budget belongs to
# the work -- drawing a diagram -- rather than to which implementation happens
# to serve it, so a caller cannot spend it twice by alternating between the Java
# exporter and the node renderer. That also means it is already on the right
# path for #273, which moves these exports to node under these same URLs.
location ~ ^/ContentService/exporter/(diagram|fireworks|document|event)/ {
    limit_req zone=render_rate burst=8 nodelay;
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}

location /ContentService/ {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://content/ContentService/;
    # A diagram or SBML export takes minutes. Apache's default is 300s and the
    # site relies on it; nginx defaults to 60 and would cut them off.
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}

location /AnalysisService/ {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://analysis/AnalysisService/;
    # An analysis over a large identifier list is not quick either.
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}

# DeltaSignal. Apache has no such route, which is why DeltaSignal cannot work on
# beta today: the Angular side calls /api/pathways, /api/parse and /api/solve as
# bare relative paths, routed only by proxy.conf.js — the dev server's proxy,
# which does not exist in a built artifact.
location /api/ {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://deltasignal/api/;
    # Solving a perturbation is not a fast request.
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}

# The chatbot. A plain proxy renders its UI and then hangs with no replies: it is
# server-sent events and needs buffering off as well as the upgrade headers. The
# Apache configuration records having hit exactly this.
location = /chat { return 302 /chat/; }
location /chat/ {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://chatbot/chat/;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering off;
    proxy_read_timeout 3600s;
}

# The MCP server (reactome-mcp), when an environment runs one.
#
# Present in every environment that includes this file, and harmless where the
# server is not running: nothing listens, nginx answers 502, and no other route
# is affected. That is the deliberate shape -- "available through nginx when it
# is on" rather than a config edit each time it is turned on or off.
#
# Streamable HTTP, so the same three settings the chat needs: buffering off, the
# upgrade headers, and a long read timeout. A response here is a stream of
# events and a proxy that buffers turns it into a long silence followed by
# everything at once.
#
# `mcp-session-id` is the header the transport identifies a session by, and it
# has to survive in both directions -- the server issues it on initialise and
# the client returns it on every later call. It does, with nothing added here:
# nginx forwards request headers unchanged and only hides a short fixed list on
# the way back (Date, Server, X-Accel-*), which this is not on. There was a
# `proxy_pass_header mcp-session-id` here on the theory that it was needed;
# removing it and re-running the initialise call returned the session id just
# the same, so it was doing nothing but asserting a danger that does not exist.
#
# The server binds 127.0.0.1 by default and turns on DNS-rebinding protection
# for localhost hosts; that protection is what stops a web page in a visitor's
# browser driving it, and it is worth not defeating by binding wider. Give the
# upstream the loopback address and let nginx be the only thing in front.
location /mcp {
    # Its own budget, and a small burst: a client legitimately sends several
    # calls in a row while a model works through a task, but not a flood.
    limit_req zone=mcp_rate burst=10 nodelay;
    # What the rate limit above does NOT do, said here because it is easy to
    # read one as the other: it bounds calls per second, not work per call.
    # Those are different quantities, and `reactome_analyze_identifiers` is
    # where they come apart -- it POSTs an identifier list to the Analysis
    # Service, which does real work and stores a result, so a single permitted
    # request could commission an arbitrarily large job. Two of those a second
    # is not a small number.
    #
    # The server caps the list itself, which is the right place for it. This is
    # a ceiling in the only unit this layer can see, so the bound does not
    # depend on which version of the server is deployed.
    #
    # 256 KB, and deliberately not a number derived from the server's identifier
    # cap. It does not bind today: the service rejects a body over 100 KiB --
    # 102,400 bytes exactly, found by bisecting against the built image, and
    # since confirmed from the other side by the team that owns it.
    #
    # That ceiling is express's default, reached through the SDK's
    # `createMcpExpressApp`, and nothing in either repository names it. The
    # service used to carry a line asking for 4 MB which never ran, because that
    # parser was mounted behind the SDK's; the line has been removed rather than
    # made to work, so 4 MB is not coming back.
    #
    # What is left for this to do is the part that does not depend on any of
    # that: an 8.8 MB body is refused at the edge rather than read into the
    # service (confirmed 413 through this configuration), and an SDK upgrade
    # that moves a default nobody declared cannot raise the ceiling past this
    # without somebody editing this line. A bound does not have to be the
    # binding one to be worth having.
    client_max_body_size 256k;
    include /etc/nginx/common/upstream-proxy.conf;
    # No trailing slash on either the location or the target, so the request
    # URI is forwarded verbatim. The server serves POST/GET/DELETE on `/mcp`
    # itself, not at its root, so the usual `location /mcp/` with
    # `proxy_pass http://mcp/;` -- which strips the prefix -- sent every call to
    # `/`, where nothing is mounted, and the transport saw a 404 it reports as a
    # connection failure. The prefix form also means a client connecting to
    # `/mcp` and one connecting to `/mcp/` both arrive.
    #
    # `/health` stays unreachable from outside, which is the other reason not to
    # proxy the upstream's root: it reports that the process is up, and that is
    # nobody else's business.
    proxy_pass http://mcp;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_buffering off;
    # A tool call can be a slow Analysis Service request, but not an hour of
    # one: the chat's 3600s is for a conversation held open, which this is not.
    proxy_read_timeout 300s;
}

# The render service. Routed here rather than left to fall through to the site
# server -- which proxies it via proxy.conf.js and would still work -- because
# a budget can only be applied where the path is named.
#
# `/RenderService/` with a trailing slash on both sides, so the prefix is
# stripped: the service serves `/render/{id}.{ext}` at its root. That matches
# what proxy.conf.js already does with `pathRewrite`.
location /RenderService/ {
    limit_req zone=render_rate burst=8 nodelay;
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://render/;
    # A large diagram takes tens of seconds and returns megabytes. The site's
    # 300s matches what the exporters needed; buffering off so a slow render
    # streams rather than being held whole in nginx first.
    proxy_buffering off;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}

# The site itself.
location / {
    include /etc/nginx/common/upstream-proxy.conf;
    proxy_pass http://site/;
    proxy_set_header Upgrade    $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_read_timeout 300s;
    proxy_send_timeout 300s;
}
