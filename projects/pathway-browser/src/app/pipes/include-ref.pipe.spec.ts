import { IncludeRefPipe } from './include-ref.pipe';

describe('IncludeRefPipe', () => {
  it('create an instance', () => {
    // const pipe = new IncludeRefPipe();
    // expect(pipe).toBeTruthy();
  });
});
