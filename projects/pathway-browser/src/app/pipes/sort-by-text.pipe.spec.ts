import { SortByTextPipe } from './sort-by-text.pipe';

describe('SortByTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByTextPipe();
    expect(pipe).toBeTruthy();
  });
});
