/*
 * Public API Surface of ngx-reactome-style
 *
 * This library is Sass only -- the theme lives in src/assets and is consumed
 * via `@use "ngx-reactome-style"`. There is deliberately no TypeScript surface;
 * the published package has always exported an empty object here.
 */
export default {};
